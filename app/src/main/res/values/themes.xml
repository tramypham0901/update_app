<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.ContactBook" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ContactBook" parent="Base.Theme.ContactBook" />

    <style name="Theme.ContactBook.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.ContactBook.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.ContactBook.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>